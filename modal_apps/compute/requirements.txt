# Deps for *running* `modal deploy` / `modal run` locally or in CI.
# (The Modal *container* image is defined inside app.py via
# `modal.Image.debian_slim().pip_install(...)` — these are for the host.)
modal
dagster-pipes
